// @flow
import styled from "styled-components";

const Text = styled.input`
  font-family: inherit;
  font-weight: inherit;
  padding: .5rem 1rem;
  cursor: text;
  color: inherit;
  border: 0;
`;

export default Text;
